= t('activity.group_group.create', owner: user_image(activity.owner, name: true)).html_safe
- if activity.trackable
  - if specification == nil
    #{link_to activity.trackable.name, activity.trackable}.
- else
  #{t'activity.group_work_group.not_exist'}.